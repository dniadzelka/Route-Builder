{
	"api": [
		{
			"hostname": "{{local}}",
			"port": "{{local}}",
			"protocol": "{{local}}",
			"prefix": "{{local}}",
			"endpoints": [
				{
					"name": "getATM",
					"endpoint": "atm_locations",
					"method": "GET"
				},
				{
					"name": "addATM",
					"endpoint": "add_atm",
					"method": "POST"
				}
			]
		}
	]
}