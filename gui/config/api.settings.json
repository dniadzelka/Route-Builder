{
	"api": [
		{
			"hostname": "{{local}}",
			"port": "{{local}}",
			"protocol": "{{local}}",
			"prefix": "{{local}}",
			"endpoints": [
				{
					"name": "test",
					"endpoint": "test",
					"method": "test"
				}
			]
		}
	]
}